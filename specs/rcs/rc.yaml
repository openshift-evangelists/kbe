apiVersion: v1
kind: ReplicationController
metadata:
  name: rchelloapp
spec:
  replicas: 1
  selector:
    app: helloapp
  template:
    metadata:
      name: myhelloapp
      labels:
        app: helloapp
    spec:
      containers:
      - name: helloapp
        image: helloapp:v1
        ports:
        - containerPort: 8080
